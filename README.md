# إنشاء مجلد جديد
mkdir eid-greeting-card
cd eid-greeting-card

# تهيئة مستودع git
git init

# إضافة الملفات
git add .

# الكمت الأول
git commit -m "Initial commit"

# إضافة المستودع البعيد
git branch -M main
git remote add origin https://github.com/abdulrahmanshaker46-sudo/eid-greeting-card.git

# رفع الملفات
git push -u origin main
